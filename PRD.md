# PRD: 학습 확인 퀴즈 생성 서비스

\---

## 1\) 목표와 성공 조건

**목표**
사용자가 학습한 텍스트를 입력하면, 기본개념 2문제 + 응용 3문제(총 5문제)를 자동 생성하고, 그 자리에서 풀고 채점까지 받을 수 있는 단일 화면 서비스를 만든다.

**성공 조건**

|항목|기준|
|-|-|
|응답 속도|텍스트 입력 → 문제 5개 생성 완료까지 평균 15초 이내|
|채점 정확도|객관식 채점 100% 정확, 주관식 채점은 사용자가 "말이 된다"고 느끼는 수준|
|개발 기간|착수부터 데모 가능 상태까지 5시간 이내|
|범위 준수|로그인, 결제, DB, 파일 저장 없이 단일 화면·단일 기능으로 완결|

\---

## 2\) 사용자 스토리

> 나는 시험/자격증을 준비하는 학습자로서,
> 공부한 내용(텍스트)을 붙여넣으면
> 내가 진짜 이해했는지 확인할 수 있는 퀴즈를 즉시 받고 싶다.
> 왜냐하면 내가 직접 문제를 내면 답을 이미 알고 있어서 이해도 점검이 안 되기 때문이다.

**핵심 시나리오**

1. 사용자가 화면에 학습 텍스트를 붙여넣는다.
2. 문제 유형(객관식/주관식/혼합)을 선택한다.
3. "퀴즈 생성" 버튼을 누른다.
4. 5개 문제가 화면에 나타나고, 사용자가 답을 작성/선택한다.
5. 제출하면 채점 결과와 해설이 표시된다.
6. 결과가 마음에 안 들면 다시 생성할 수 있다.

\---

## 3\) 화면 명세 (단일 화면)

화면은 하나이며, 아래 4개 영역이 순서대로 위에서 아래로 배치된다. (라우팅/페이지 전환 없음, 같은 화면 내 상태 변화로 처리)

### 영역 A. 입력 영역

* 텍스트 입력창 (여러 줄 입력 가능, placeholder: "공부한 내용을 붙여넣어 주세요")
* 문제 유형 선택 UI (라디오 버튼 또는 세그먼트 컨트롤): `객관식` / `주관식` / `객관식+주관식 혼합`
* "퀴즈 생성" 버튼

### 영역 B. 로딩 영역

* 버튼 클릭 후 문제가 생성되는 동안 노출
* 로딩 인디케이터 + 안내 문구 "조금만 기다려주세요."
* 이 영역은 로딩 중에만 보이고, 완료되면 사라짐

### 영역 C. 문제/응답 영역

* 문제 5개가 순서대로 표시 (문제1\~2: 기본개념, 문제3\~5: 응용 — 화면상 구분 라벨 표시, 예: "기본개념" / "응용")
* 객관식 문제: 선택지 4개 중 하나 선택 (라디오 형태)
* 주관식 문제: 텍스트 입력창
* 하단에 "채점하기" 버튼

### 영역 D. 결과 영역

* "채점하기" 클릭 시 노출
* 문제별로: 사용자 답안 / 정답 여부(O,X) / 정답 및 해설
* 전체 점수(예: 5문제 중 3문제 정답)
* "다시 만들기" 버튼 (영역 A로 초기화, 입력 텍스트는 유지하거나 비우는 것은 구현 재량)

\---

## 4\) 기능 명세

기능은 **하나**다: *"학습 텍스트를 입력받아 5문제 퀴즈를 생성하고, 사용자의 답안을 채점하여 해설과 함께 보여준다."*

이 하나의 기능은 아래 세 단계로 구성된다.

### 4-1. 문제 생성

* 입력값: 학습 텍스트(문자열), 문제 유형(객관식/주관식/혼합)
* 처리: 입력 텍스트를 기반으로 기본개념 2문제 + 응용 3문제, 총 5문제를 생성한다.

  * 혼합 선택 시: 5문제 중 객관식과 주관식이 섞이도록 생성 (예: 3:2 또는 생성 로직 재량)
* 출력값: 문제 5개 각각에 대해 — 문제 번호, 문제 유형(기본개념/응용), 문제 텍스트, (객관식인 경우) 선택지 4개, 정답, 해설

### 4-2. 답안 입력

* 사용자가 화면에서 문제별로 답을 선택하거나 작성한다.
* 모든 문제에 답을 하지 않아도 채점 시도는 가능해야 한다 (미응답은 오답 처리, 예외처리 5-5 참고).

### 4-3. 채점 및 해설 제공

* 입력값: 문제 5개 + 정답 5개 + 사용자 답안 5개
* 처리:

  * 객관식: 선택지가 정답과 일치하는지 단순 비교
  * 주관식: 사용자 답안이 정답의 핵심 의미를 포함하는지 판단하여 정답/오답 처리
* 출력값: 문제별 정답 여부, 정답, 해설, 총점

\---

## 5\) 예외 처리 (상세)

> 이 서비스는 DB도, 로그인도, 재시도 큐도 없는 단일 화면 서비스이므로, 모든 예외는 \*\*그 화면 안에서 즉시 사용자에게 상태를 보여주고, 사용자가 스스로 다음 행동을 할 수 있게\*\* 하는 것을 원칙으로 한다.

### 5-1. 입력이 비어있는 경우

* **트리거**: 사용자가 텍스트를 입력하지 않고 "퀴즈 생성" 버튼을 클릭
* **처리**: 퀴즈 생성을 시도하지 않는다. 입력창 근처에 "학습 내용을 입력해 주세요"라는 안내 문구를 노출한다.
* **UI 상태**: 입력창에 포커스를 자동으로 이동시키는 것을 권장 (필수는 아님)
* **복구**: 사용자가 텍스트를 입력하면 안내 문구는 사라진다.

### 5-2. 입력 텍스트가 너무 짧은 경우

* **트리거**: 입력된 텍스트가 문제 5개(기본개념 2 + 응용 3)를 만들기에 정보량이 부족하다고 판단되는 경우 (예: 특정 글자 수 미만, 기준값은 구현 시 정함 — 예: 100자 미만)
* **처리**: 퀴즈 생성을 시도하지 않는다. "학습 내용을 조금 더 길게 써주세요"라는 안내 문구를 노출한다.
* **복구**: 사용자가 텍스트를 추가 입력하고 다시 버튼을 누르면 재시도된다.

### 5-3. 입력 텍스트가 너무 긴 경우 *(추가 정의 필요 항목)*

* **트리거**: 텍스트 길이가 처리 가능한 상한(예: 3,000자)을 초과
* **처리**: 상한을 초과했다는 안내 문구("입력이 너무 깁니다. OOO자 이내로 줄여주세요")를 노출하고 생성을 시도하지 않거나, 상한까지만 잘라서 처리한다는 것을 사용자에게 명시한다. (둘 중 하나로 정책을 확정해 구현할 것)
* **복구**: 텍스트를 줄이면 재시도 가능

### 5-4. 문제 생성 응답이 오래 걸리는 경우

* **트리거**: "퀴즈 생성" 클릭 후 응답 대기 상태
* **처리**: 로딩 인디케이터와 함께 "조금만 기다려주세요." 안내 문구를 화면에 표시한다. 이 상태에서는 "퀴즈 생성" 버튼을 다시 누를 수 없도록 비활성화한다 (중복 요청 방지).
* **추가 기준**: 일정 시간(예: 30초) 이상 응답이 없을 경우, "응답이 지연되고 있습니다. 다시 시도해 주세요"라는 문구와 함께 재시도 버튼을 노출한다.
* **복구**: 재시도 버튼 클릭 시 5-1\~5-2 검증부터 다시 수행 후 재요청

### 5-5. 문제/답안 생성 결과가 이상하거나 실패한 경우 (다시 할 수 있는가)

* **트리거**: 아래 세 가지 하위 케이스 포함

  * (a) API/모델 호출 자체가 실패한 경우 (오류 응답)
  * (b) 문제가 5개보다 적게 생성되거나 형식이 깨진 경우 (예: 선택지 누락)
  * (c) 사용자가 보기에 문제 품질이 이상하다고 판단하는 주관적인 경우
* **처리**:

  * (a), (b)는 시스템이 자동 감지하여 "문제 생성에 실패했습니다. 다시 시도해 주세요"라는 문구와 함께 재시도 버튼을 노출한다.
  * (c)는 시스템이 감지할 수 없으므로, 결과 화면에 항상 "다시 만들기" 버튼을 상시 노출하여 사용자가 언제든 재생성을 요청할 수 있게 한다.
* **복구**: 재시도 시 이전 입력 텍스트는 유지한 채로 문제 생성 단계(4-1)부터 다시 수행한다.

### 5-6. 사용자가 일부 문제만 답하고 채점을 시도하는 경우

* **트리거**: 5문제 중 일부만 답한 상태로 "채점하기" 클릭
* **처리**: 채점을 막지 않는다. 답하지 않은 문제는 오답으로 처리하고, 결과 화면에 "미응답"으로 표기한다. (경고 문구로 "아직 답하지 않은 문제가 있습니다"를 채점 전에 한 번 보여주는 것은 선택 사항)
* **복구**: 채점 이후에도 "다시 만들기"로 새 퀴즈 요청 가능 (기존 답안 수정 후 재채점 기능은 범위에서 제외 — 안 만들 것 항목 참고)

### 5-7. 새로고침/이탈 시 데이터 유실

* **트리거**: 사용자가 브라우저를 새로고침하거나 화면을 벗어남
* **처리**: 별도 복구 로직 없이 모든 상태(입력 텍스트, 생성된 문제, 답안)가 초기화된다. 이는 DB 미사용 정책에 따른 의도된 동작이며, 별도의 저장/경고 팝업("나가시겠습니까?")도 만들지 않는다.
* **비고**: 이는 "안 만들 것" 정책(DB/영구 저장 없음)과 직접 연결되므로 버그가 아니라 설계된 동작임을 코드 주석 등에 명시할 것을 권장한다.

\---

## 6\) 완료 조건 (Definition of Done)

* \[ ] 단일 화면에서 입력 → 유형 선택 → 생성 → 풀이 → 채점 → 해설까지 전 과정이 페이지 이동 없이 동작한다.
* \[ ] 기본개념 2문제 + 응용 3문제, 총 5문제가 매 요청마다 생성된다.
* \[ ] 객관식/주관식/혼합 3가지 유형 선택이 모두 정상 동작한다.
* \[ ] 5-1\~5-7의 예외 상황이 모두 화면에서 재현 및 확인 가능하다.
* \[ ] 로그인, 결제, DB, 파일 업로드/저장 기능이 코드 어디에도 존재하지 않는다.
* \[ ] 텍스트 입력부터 문제 생성 완료까지 평균 15초 내외로 동작한다 (환경에 따라 변동 가능, 목표치로 취급).
* \[ ] "다시 만들기" 버튼으로 몇 번이든 반복 사용 가능하다.

